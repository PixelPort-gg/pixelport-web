<div class="portal-frame">
  <div class="portal-threshold" aria-hidden="true"></div>
  <div class="portal-radiance" aria-hidden="true"></div>
  <div class="portal-content"><slot /></div>
</div>

<style>
  @property --portal-energy {
    syntax: '<number>';
    inherits: true;
    initial-value: 0;
  }

  .portal-frame {
    --portal-energy: 0;
    position: relative;
    isolation: isolate;
    overflow: hidden;
    container-type: inline-size;
    min-width: 0;
    border: 1px solid rgba(146, 104, 255, 0.28);
    border-radius: 16px;
    background:
      radial-gradient(
        circle at 48% 50%,
        rgb(146 104 255 / calc(0.04 + var(--portal-energy) * 0.2)),
        transparent 31%
      ),
      radial-gradient(
        circle at 18% 30%,
        rgb(146 104 255 / calc(0.13 + var(--portal-energy) * 0.05)),
        transparent 34%
      ),
      radial-gradient(
        circle at 82% 70%,
        rgb(63 214 206 / calc(0.09 + var(--portal-energy) * 0.07)),
        transparent 36%
      ),
      #090717;
  }

  .portal-frame::before {
    position: absolute;
    inset: 0;
    z-index: 3;
    border-radius: inherit;
    background:
      linear-gradient(90deg, rgba(146, 104, 255, 0.7), transparent 28%) top left / 42% 1px no-repeat,
      linear-gradient(270deg, rgba(63, 214, 206, 0.72), transparent 28%) bottom right / 42% 1px no-repeat;
    content: '';
    pointer-events: none;
  }

  .portal-threshold {
    position: absolute;
    top: -14%;
    bottom: -14%;
    left: 50%;
    z-index: -1;
    width: 2px;
    background: linear-gradient(180deg, transparent, var(--color-accent), #7d89e9 48%, var(--color-accent-2), transparent);
    box-shadow:
      0 0 20px 3px rgba(146, 104, 255, 0.24),
      0 0 54px 14px rgba(63, 214, 206, 0.08);
    opacity: 0.82;
    transform: rotate(7deg);
  }

  .portal-radiance {
    position: absolute;
    inset: 18% 32%;
    z-index: -2;
    border-radius: 50%;
    background: linear-gradient(90deg, rgba(146, 104, 255, 0.2), rgba(63, 214, 206, 0.13));
    filter: blur(46px);
    pointer-events: none;
  }

  .portal-content {
    position: relative;
    z-index: 1;
    min-height: inherit;
  }
</style>
